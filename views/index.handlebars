<!--div to hold the images for the burger-->
<div class="container">
   <img src="/assets/images/burger5.png" class="burgerpix">
    <h1 id="title">Patty Maker!</h1>
</div>
<div class="container">
    <div class="row">        
        <div class="col-lg-4"><ol>
            {{#each burger}}
            {{#if devoured}}
            <div class='row'>
                <div class='col-lg-12'>
                    <li class='list'>
                        <p class='wait'>{{this.id}}. {{this.burger_name}}</p>
                        <button class='check' data-id="{{this.id}}">Consume That Bad Boy!</button>
                        </li>
                        </div>
                        </div>
            {{/if}}
            {{/each}}
        </ol>
        </div>
        <div class="col-lg-4">                    
<form id="burger" style="text-align: center;">
    <textarea id="burgername" name="burgername">
    </textarea><br>
    <button type="submit"class="btn btn-primary" id="getData">create</button>
</form>
<div class="row">
<div class="col-lg-12">
    <h6  style="text-align: center;font-weight: bold;">Click Submit To Creat A New Crabby Patty !</h6>
</div>
</div>
    </div>    
<div class="col-lg-4"><ol>
     {{#each burger}}
            {{#unless devoured}}
            <div class='row'>
                <div class='col-lg-12'>
                    <li class='list'>
                        <p class='nowait'>{{this.id}}. {{this.burger_name}}</p>
                        </li>
                        </div>
                        </div>
            {{/unless}}
            {{/each}}
</ol></div>
</div>
</div>
